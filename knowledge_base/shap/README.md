# SHAP Store

Global SHAP analysis for feature importance context.

## Files
- `global_shap.json` - Mean |SHAP| values by feature

## Note
Local SHAP is computed at runtime and NOT stored here.

## Purpose
Provides baseline for comparing individual predictions to population.
